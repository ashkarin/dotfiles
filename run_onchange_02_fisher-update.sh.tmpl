#!/bin/bash
set -eufo pipefail

{{- if (eq .chezmoi.os "darwin") }}

# hash of the fish_plugins file, used to determine if we need to rerun this script
# fish/fish_plugins hash: {{ include "dot_config/fish/fish_plugins" | sha256sum }}

fish -c "fisher update"

{{ end -}}